export interface FormData {
  name: string;
  email: string;
  niche: string;
}

export interface ValueAssessment {
  customerService: number;
  virtualReceptionist: number;
  appointmentSetter: number;
  onboarding: number;
  workflow: number;
}